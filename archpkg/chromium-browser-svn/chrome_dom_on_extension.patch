Index: src/chrome/browser/extensions/extension_dom_ui.cc
===================================================================
--- src/chrome/browser/extensions/extension_dom_ui.cc	(revision 39074)
+++ src/chrome/browser/extensions/extension_dom_ui.cc	(working copy)
@@ -24,7 +24,9 @@
   // TODO(aa): It would be cool to show the extension's icon in here.
   hide_favicon_ = true;
   should_hide_url_ = true;
-  bindings_ = BindingsPolicy::EXTENSION;
+  // Wei: Allows extension page to call chrome.send
+  // Look at the internal pages under chrome/browser/resources/ for usage.
+  bindings_ = BindingsPolicy::EXTENSION | BindingsPolicy::DOM_UI;
 
   // For chrome:// overrides, some of the defaults are a little different.
   GURL url = tab_contents->GetURL();
