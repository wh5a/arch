Index: src/third_party/WebKit/Source/WebCore/page/DOMWindow.cpp
===================================================================
--- src/third_party/WebKit/Source/WebCore/page/DOMWindow.cpp	(revision 102542)
+++ src/third_party/WebKit/Source/WebCore/page/DOMWindow.cpp	(working copy)
@@ -1444,6 +1444,25 @@
     view->scrollBy(IntSize(x, y));
 }
 
+void DOMWindow::scrollByPages(int pages) const
+{
+    if (!m_frame)
+        return;
+
+    m_frame->document()->updateLayoutIgnorePendingStylesheets();
+
+    FrameView* view = m_frame->view();
+    if (!view)
+        return;
+
+    if (pages > 0)
+      for (; pages > 0; pages--)
+        view->scroll(ScrollDown, ScrollByPage);
+    else
+      for (; pages < 0; pages++)
+        view->scroll(ScrollUp, ScrollByPage);
+}
+
 void DOMWindow::scrollTo(int x, int y) const
 {
     if (!isCurrentlyDisplayedInFrame())
Index: src/third_party/WebKit/Source/WebCore/page/DOMWindow.h
===================================================================
--- src/third_party/WebKit/Source/WebCore/page/DOMWindow.h	(revision 102542)
+++ src/third_party/WebKit/Source/WebCore/page/DOMWindow.h	(working copy)
@@ -236,6 +236,7 @@
         void postMessageTimerFired(PassOwnPtr<PostMessageTimer>);
 
         void scrollBy(int x, int y) const;
+        void scrollByPages(int pages) const;
         void scrollTo(int x, int y) const;
         void scroll(int x, int y) const { scrollTo(x, y); }
 
Index: src/third_party/WebKit/Source/WebCore/page/DOMWindow.idl
===================================================================
--- src/third_party/WebKit/Source/WebCore/page/DOMWindow.idl	(revision 102542)
+++ src/third_party/WebKit/Source/WebCore/page/DOMWindow.idl	(working copy)
@@ -107,6 +107,7 @@
         readonly attribute long pageYOffset;
 
         void scrollBy(in [Optional=CallWithDefaultValue] long x, in [Optional=CallWithDefaultValue] long y);
+        void scrollByPages(in [Optional=CallWithDefaultValue] long pages);
         void scrollTo(in [Optional=CallWithDefaultValue] long x, in [Optional=CallWithDefaultValue] long y);
         void scroll(in [Optional=CallWithDefaultValue] long x, in [Optional=CallWithDefaultValue] long y);
         void moveBy(in [Optional=CallWithDefaultValue] float x, in [Optional=CallWithDefaultValue] float y); // FIXME: this should take longs not floats.
