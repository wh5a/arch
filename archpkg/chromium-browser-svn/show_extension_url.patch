Index: src/chrome/browser/extensions/extension_dom_ui.cc
===================================================================
--- src/chrome/browser/extensions/extension_dom_ui.cc	(revision 41808)
+++ src/chrome/browser/extensions/extension_dom_ui.cc	(working copy)
@@ -48,7 +48,7 @@
 
 ExtensionDOMUI::ExtensionDOMUI(TabContents* tab_contents)
     : DOMUI(tab_contents) {
-  should_hide_url_ = true;
+  should_hide_url_ = false;
   bindings_ = BindingsPolicy::EXTENSION;
 
   // For chrome:// overrides, some of the defaults are a little different.
Index: src/chrome/browser/extensions/extension_host.cc
===================================================================
--- src/chrome/browser/extensions/extension_host.cc	(revision 41775)
+++ src/chrome/browser/extensions/extension_host.cc	(working copy)
@@ -130,6 +130,8 @@
       dom_storage_context()->AllocateSessionStorageNamespaceId();
   render_view_host_ = new RenderViewHost(site_instance, this, MSG_ROUTING_NONE,
                                          session_storage_namespace_id);
+  // Wei: granting extension bindings and dom_ui bindings simultaneously confuses chrome: we lose the extension bindings
+  // render_view_host_->AllowBindings(BindingsPolicy::EXTENSION | BindingsPolicy::DOM_UI);
   render_view_host_->AllowBindings(BindingsPolicy::EXTENSION);
   if (enable_dom_automation_)
     render_view_host_->AllowBindings(BindingsPolicy::DOM_AUTOMATION);
